import { check, famInput, ismInput } from "./let"


export function inport(ism,fam,guruh,jins) {
   return {
    ism:ismInput.value,
    fam:famInput.value,
    guruh:sinf.value,
    jins:check.value
   } 
}